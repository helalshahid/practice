// Create a log in reducer:

const intialState = {
  isLoogedIn: false,
};

export const loginReducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
